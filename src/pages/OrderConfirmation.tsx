import { Link, useSearchParams } from "react-router-dom";
export default function OrderConfirmation() { const [params] = useSearchParams(); return <div className="mx-auto max-w-xl px-6 py-24 text-center"><span className="eyebrow">Merci</span><h1 className="mt-4 font-display text-3xl">Order confirmed</h1><p className="mt-5 text-midnight/60">Your order has been received{params.get("id") ? ` (${params.get("id")})` : ""}. We will contact you to confirm delivery.</p><div className="mt-9 flex justify-center gap-4"><Link to="/orders" className="border border-line px-6 py-4 text-xs uppercase tracking-[0.2em]">View orders</Link><Link to="/shop" className="bg-midnight px-6 py-4 text-xs uppercase tracking-[0.2em] text-ivory">Continue shopping</Link></div></div>; }
